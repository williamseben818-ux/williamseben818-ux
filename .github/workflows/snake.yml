name: Seasonal Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"   # runs once a day
  workflow_dispatch:       # allows manual trigger

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v2
- name: Generate Birthday Snake
  if: ${{ format('%m', github.event.schedule) == '12' }}
  uses: Platane/snk@master
  with:
    github_user_name: williamseben818-ux
    outputs: output/snake-birthday.svg
    snake_color: "#00FFFF,#FF00FF,#39FF14,#9400D3"   # neon palette
    background: "output/fireworks-bg.svg"            # glowing background
    speed: 5
